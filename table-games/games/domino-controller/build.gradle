group 'com.simplejcode.var.table-games'

dependencies {
    compile project(':table-games:games:domino-base')
    compile project(':table-games:game-controller')
}

configurations {
    testCompile.extendsFrom junit
}
